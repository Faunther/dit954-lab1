
# Temporär fil för att hålla reda på kvarstående del-uppgifter
================================================================
* Investigate/Fix DrawPanel
    ska den hålla all "JPanel" data, och vill vi göra en ny klass?

* Implementera CarSystemFactory
    Ska Factory ha en referens till IViewActionHandler, (DimensionManager?), etc
    så att vi kan lägga till dem där direkt när de skapas, eller ska vi göra bindningen explicit utanför?


# Önskvärt (?)
================================================================
* DimensionManager (Collider/Physics-LUT) (LUT = Look-Up Table)
    Vi behöver ha någon referens till hur stora föremål är, jag föreslår en "wrapped" dictionary

    // Example
    /*
    interface ISizeLUT {
        int getWidth(int id);
    }

    class CarSizeImpl implements ISizeLUT {
        HashMap<Integer, Integer> m_sizes = new HashMap();

        public CarSizeImpl() {
            // load images for their sizes,
            // store them under their corresponding entries
            // throw away the images.

            BufferedImage volvoImage = ImageIO.read(DrawPanel.class.getResourceAsStream("pics/Volvo240.jpg"));
            m_sizes.put(240, volvoImage.getWidth());
        }

        public int getWidth(int id) {
            return m_sizes.get(id);
        }
    }
    * */





* IViewActionHandler::addSubscriber() variant
    Vi skulle kunna ha flera namngivna (ej overload) "addSubscribers" istället
    då behöver vi inte ha flera "if (cs instanceof ITurboSubscriber)..."